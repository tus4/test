{
  "_comment": "Copy to dashboard.config.json at the ROOT of the private data repository (tus4/data1). Every key is optional; keys starting with '_' are ignored. Unknown keys fail the build.",
  "mode": "rows",
  "_mode": "\"rows\" = publish data rows (capped) plus statistics and charts; \"aggregates\" = statistics and charts only, never rows.",
  "title": "데이터 대시보드",
  "include_files": ["**/*.xlsx", "**/*.xlsm"],
  "exclude_files": ["backup/**", "**/*_draft.xlsx"],
  "include_sheets": [],
  "exclude_sheets": ["원본*", "raw"],
  "include_columns": [],
  "exclude_columns": ["*메모*", "내부*"],
  "auto_exclude_sensitive": true,
  "_auto_exclude_sensitive": "Columns whose header contains one of sensitive_patterns, or whose values look like e-mail / phone / resident-id, are dropped automatically. List a header in allow_columns to publish it anyway.",
  "sensitive_patterns": ["주민", "여권", "전화", "휴대폰", "핸드폰", "연락처", "이메일", "메일주소", "주소", "계좌", "카드번호", "비밀번호", "생년월일", "email", "e-mail", "phone", "mobile", "address", "account no", "card number", "password", "passwd", "ssn", "passport", "birth"],
  "allow_columns": [],
  "max_rows": 1000,
  "max_columns": 50,
  "max_scan_rows": 200000,
  "max_cell_text": 200,
  "preview_rows": 10,
  "preview_columns": 12,
  "header_scan_rows": 20,
  "max_chart_categories": 12,
  "charts": true,
  "publish_top_values": true,
  "readme": { "charts": true, "xychart": false, "preview": true },
  "fail_if_no_workbooks": true,
  "_timeseries": "Wide daily time-series sheets (a DATES / 일자 header row followed by dated rows; optional Category / Ticker / Notation rows above it) are published in full as the market dashboard (index.html). highlights = KPI tiles, featured = overview charts (mode: level | diff | rebase), clean_outliers = blank zeros that mean 'no quote' and one-day spikes.",
  "timeseries": {
    "enabled": true,
    "highlights": ["한국_기준금리", "한국_3Y", "한국_10Y", "UST2Y", "UST10Y", "USDKRW"],
    "featured": [
      { "title": "한·미 국채 금리", "series": ["한국_3Y", "한국_10Y", "UST2Y", "UST10Y"] },
      { "title": "주요 환율 (기간 초 = 100)", "series": ["USDKRW", "USDJPY", "DXY"], "mode": "rebase" }
    ],
    "clean_outliers": true,
    "spark_points": 52,
    "max_series": 1000
  }
}
